Use this prompt with your Replit agent:

I deployed this app to a Raspberry Pi and `npm install` completed, but `npm start` fails. Please inspect the codebase and build configuration for issues that could cause this runtime error and fix them if they are in the project code or config.

Current runtime output on the Pi:

* `Using SQLite storage (self-hosted environment)`
* `SQLite database initialized`
* `No DATABASE_URL found, using memory session store`
* `WARNING: No SESSION_SECRET set. Using random secret - sessions will not persist across restarts.`
* Then it crashes with:
* `Error: Dynamic require of "crypto" is not supported`
* stack trace points to:

  * `dist/index.js:9:9`
  * `dist/index.js:1606:12`
  * `dist/index.js:1607:5`

What I need you to do:

1. Inspect the server-side code and build output path that produces `dist/index.js`.
2. Determine whether the problem is caused by:

   * ESM vs CommonJS incompatibility
   * a bundler transforming Node built-ins incorrectly
   * use of dynamic `require()` in server code or dependencies
   * a bad `package.json` start/build configuration
   * importing Node’s `crypto` module incorrectly for the current module format
3. Check all relevant files, especially:

   * `package.json`
   * server entry file
   * build scripts
   * tsconfig
   * vite / esbuild / tsup / bundler config if present
4. Search the codebase for:

   * `require("crypto")`
   * `require('crypto')`
   * `import crypto`
   * `import * as crypto`
   * `createRequire`
   * any dynamic require patterns
5. Confirm whether `dist/index.js` is being bundled for Node correctly. If built-in Node modules like `crypto` are being inlined, wrapped, or transformed in a way that breaks Node ESM, fix that.
6. If the issue is caused by code or config, update the project so the production build can run on a Raspberry Pi using Node directly.
7. If environment variables are required, clearly list them, but focus first on the crash caused by `crypto`.
8. After fixing, give me:

   * a summary of the root cause
   * the exact files changed
   * the updated npm commands I should run on the Pi
   * whether I should use `npm start`, a different start command, or rebuild first

Important:

* Do not just give generic advice.
* Trace the actual source of the `crypto` error.
* Assume the Pi setup itself is mostly fine and focus on code/build issues inside the project.
* If the problem is only in the built output, trace back to the source config that generated it.

You can also add this at the end if you want the agent to be more aggressive about fixing it:

If you find more than one possible cause, prioritize the fix that is most robust for production deployment on Linux/Raspberry Pi with Node 20.
