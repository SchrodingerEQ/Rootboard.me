# Dependencies
node_modules

# Build output
dist
server/public

# OS
.DS_Store

# Vite
vite.config.ts.*

# Tarballs
*.tar.gz

# Environment variables (secrets!)
.env
.env.*
!.env.example

# Google service account key file (contains a private key!)
service-account.json

# Google OAuth credentials (legacy — pre-service-account flow; kept gitignored for users migrating)
google_credentials.json

# Self-hosted database and runtime data
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Auto-update runtime folders
.update-backups/
.update-temp/

# Replit/agent debugging artifacts (contain personal screenshots, hostnames, IPs, OAuth screenshots)
# Exclude the entire folder EXCEPT the one logo image actually imported by the app
# (the @assets alias in vite.config.ts points to attached_assets/).
attached_assets/*
!attached_assets/image_1753142842256.png
.local/
.agents/
.cache/
.upm/

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
